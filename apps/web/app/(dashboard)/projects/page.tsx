import ProjectsListPanel from '@/components/projects/ProjectsListPanel';

/** Projects list — top-level `/projects` (was `/dashboard/projects`). */
export default function ProjectsPage() {
  return <ProjectsListPanel />;
}
