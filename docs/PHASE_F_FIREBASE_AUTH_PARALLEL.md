# Phase F — Firebase Auth Parallel Implementation

**Status:** Complete (parallel mode, Supabase fallback preserved)  
**Flag:** `USE_FIREBASE_AUTH` / `NEXT_PUBLIC_USE_FIREBASE_AUTH` (default **off**)

See migration plan §F. Identity verification lives in `@paperworking/identity`. Application authorization unchanged in `@paperworking/authz`.
