{
  "name": "paperworking-migration",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated architecture migration for PaperWorking (read-only source: ../PaperWorking)",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tests/*"
  ],
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "start": "npm --prefix apps/web run start",
    "start:api": "npm run start:api --workspace=@paperworking/api",
    "dev:api": "npm run dev:api --workspace=@paperworking/api",
    "build:libs": "npm --prefix packages/shared run build && npm --prefix packages/validation run build && npm --prefix packages/financial-engine run build && npm --prefix packages/database run build",
    "build": "npm run build:libs && npm --prefix apps/api run build && npm --prefix apps/web run build",
    "apphosting:build": "bash scripts/apphosting-build.sh",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "verify:integration": "npm run test --workspace=@paperworking/integration",
    "test:e2e": "npm run test:e2e --workspace=@paperworking/e2e",
    "verify": "npm run build && npm run typecheck --workspaces --if-present && npm run test --workspaces --if-present",
    "deploy:api": "bash scripts/deploy-api-cloud-run.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.0",
    "typescript": "^5.8.3"
  }
}
