{
  "name": "@paperworking/integration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phase 6 — cross-stack integration verification (handlers + adapters + sandbox flows)",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --experimental-vm-modules ../../node_modules/jest/bin/jest.js"
  },
  "dependencies": {
    "@paperworking/api": "*",
    "@paperworking/financial-engine": "*",
    "@paperworking/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
