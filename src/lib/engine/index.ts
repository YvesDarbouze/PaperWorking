/**
 * CommunicationEngine — Public re-exports
 *
 * Import from '@/lib/engine' for clean consumption:
 *   import { CommunicationEngine } from '@/lib/engine';
 *   import type { TemplateSlug, EmailStatus, SendResult } from '@/lib/engine';
 */
export {
  CommunicationEngine,
  type TemplateSlug,
  type EmailStatus,
  type ContextData,
  type SendResult,
} from './CommunicationEngine';
